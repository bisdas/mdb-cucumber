import React, { type FunctionComponent, type ReactElement } from 'react'
import { OuterWrapper } from './BlogPage.styled'

const BlogPage: FunctionComponent<any> = (): ReactElement => {
    return <OuterWrapper>Blog Page</OuterWrapper>
}

export default BlogPage
