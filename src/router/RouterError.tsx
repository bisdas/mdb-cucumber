import React, { type FunctionComponent, type ReactElement } from 'react'

export const RouterError: FunctionComponent<any> = (): ReactElement => {
    return (
        <div>
            <div>This is Router Error component</div>
        </div>
    )
}
